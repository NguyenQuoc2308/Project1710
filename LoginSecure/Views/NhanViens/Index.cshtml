@model IEnumerable<LoginSecure.Models.NhanVien>

@{
    ViewBag.Title = "Index";
}
<section class="content-header">
    <h2>Index</h2>
</section>
<section class="content">
    <div class="box">
        <div class="box-header">
            <h3>Nhan Vien Table</h3>
            <p>
                @Html.ActionLink("Create New", "Create")
            </p>
        </div>
        <div class="box-body">
            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.TenNV)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.GioitinhNV)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.TenNV)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.GioitinhNV)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.MaNV }) |
                                @Html.ActionLink("Details", "Details", new { id = item.MaNV }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.MaNV })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>
